>

int main() {
    int a[10],b[10],c[25];
    int i,j,k,n1,n2;
    scanf("%d",&n1);
    for(i=0;i<n1;i++)
    scanf("%d",&a[i]);
    scanf("%d",&n2);
    for(j=0;j<n2;j++)
    scanf("%d",&b[j]);
    i=0;
    j=0;
    k=0;
    while(i<n1&&j<n2)
    {
        if(a[i]<=b[j])
        {
        c[k]=a[i];
        i++;
        k++;
        }
        else
        {
            c[k]=b[j];
            k++;
            j++;
        }
    }
    return 0;
}
